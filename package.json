{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn wsrun --stages --exclude-missing build",
    "watch": "yarn wsrun --stages --exclude-missing watch"
  },
  "dependencies": {
    "wsrun": "^5.2.1"
  }
}
